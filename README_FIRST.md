# TATA MOTORS Operations Portal

This TATA MOTORS operations system converts shop data entry into two visible Excel-style outputs:

1. `Meeting Excel Sheet` — the presentable meeting format with formulas similar to the screenshot.
2. `Analytical Sheet` — shop/month/day/cumulative/breakdown/parameter analytics.

The hidden `Main Data` sheet is used only as the database for the app.
